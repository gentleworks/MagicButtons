import TouchKit

// Visualizer — SwiftUI finger dots + zone bands (docs/06-visualizer.md).
//
// The live view is `VisualizerView` over `VisualizerModel` (Phase 5). This enum
// only carries the module-name marker used by the App scaffold's smoke check.
public enum Visualizer {
    public static let moduleName = "Visualizer"
}
